{
  "name": "comedor-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web-admin\"",
    "dev:cliente": "npm run dev --workspace=apps/web-cliente",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/web-admin && npm run build --workspace=apps/web-cliente",
    "lint": "npm run lint --workspace=apps/api && npm run lint --workspace=apps/web-admin",
    "typecheck": "npm run typecheck --workspace=apps/api && npm run typecheck --workspace=apps/web-admin",
    "test": "npm run test --workspace=apps/api",
    "db:migrate": "npm run db:migrate --workspace=packages/database",
    "db:deploy": "cd packages/database && npx prisma migrate deploy",
    "db:generate": "npm run db:generate --workspace=packages/database",
    "db:seed": "npm run db:seed --workspace=packages/database"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
